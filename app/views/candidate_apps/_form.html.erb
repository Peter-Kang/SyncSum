<%= form_for(@candidate_app) do |f| %>
  <% if @candidate_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate_app.errors.count, "error") %> prohibited this candidate_app from being saved:</h2>

      <ul>
      <% @candidate_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :candidate_id %><br />
    <%= f.number_field :candidate_id %>
  </div>
  <div class="field">
    <%= f.label :position_id %><br />
    <%= f.number_field :position_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
